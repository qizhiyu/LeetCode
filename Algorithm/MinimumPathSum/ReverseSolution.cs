using System;

using System.Linq;
using System.Collections.Generic;

public class ReverseSolution
{
    public int MinPathSum(int[][] grid)
    {
        var m = grid.Length;
        var n = grid[0].Length;

        if (m == 0 || m == 0) return 0;

        for (int i = m - 1; i >= 0; i--)
        {
            for (int j = n - 1; j >= 0; j--)
            {

            }
        }
    }

}